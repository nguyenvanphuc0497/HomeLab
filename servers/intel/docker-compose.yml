name: homelab-intel

x-defaults: &defaults
  restart: unless-stopped

include:
  - ../../services/exporters/docker-compose.yml
  - ../../services/gpu-exporters/docker-compose.yml

services:
  # Placeholder for Intel specific services if needed
  dummy:
    image: alpine:3.19
    command: [ "echo", "Intel Node Monitoring Agent Active" ]

networks:
  monitoring:
    name: monitoring
    driver: bridge
