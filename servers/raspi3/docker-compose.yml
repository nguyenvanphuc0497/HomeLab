name: homelab-raspi3

x-defaults: &defaults
  restart: unless-stopped

services:
  placeholder:
    <<: *defaults
    env_file:
      - .env
    image: alpine:latest
    platform: linux/arm/v7
    command: ["/bin/sh", "-c", "sleep infinity"]
    # TODO: replace with services pinned for Raspberry Pi 3 (armv7)

