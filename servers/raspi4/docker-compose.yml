name: homelab-raspi4

x-defaults: &defaults
  restart: unless-stopped

include:
  - ../../services/gitea/docker-compose.yml

services:
  # Main entry point for this node
  # Additional node-specific services can be defined here if needed
  node-agent:
    <<: *defaults
    image: alpine:latest
    command: ["/bin/sh", "-c", "echo 'Node active' && sleep infinity"]

