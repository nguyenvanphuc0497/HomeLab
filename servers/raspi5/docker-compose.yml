name: homelab-raspi5

x-defaults: &defaults
  restart: unless-stopped

include:
  - ../../services/gitea/docker-compose.yml
  - ../../services/coolify/docker-compose.yml
  - ../../services/monitoring/docker-compose.yml
  - ../../services/exporters/docker-compose.yml
  - ../../services/wg-easy/docker-compose.yml
  - ../../services/stirling-pdf/docker-compose.yml
  - ../../services/it-tools/docker-compose.yml
  - ../../services/excalidraw/docker-compose.yml

services:
  # Main entry point for this node (Raspberry Pi 5)
  # High performance node for heavy workloads
  node-agent:
    <<: *defaults
    image: alpine:latest
    command: [ "/bin/sh", "-c", "echo 'Node active' && sleep infinity" ]

networks:
  monitoring:
    name: monitoring
    driver: bridge
