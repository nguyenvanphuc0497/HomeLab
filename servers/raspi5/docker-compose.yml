name: homelab-raspi5

x-defaults: &defaults
  restart: unless-stopped

include:
  # VPN
  - path: ../../services/wg-easy/docker-compose.yml

  # Smart Home
  - path: ../../services/homebridge/docker-compose.yml

  # Development & Deployment
  - path: ../../services/gitea/docker-compose.yml
  - path: ../../services/coolify/docker-compose.yml
  - path: ../../services/stirling-pdf/docker-compose.yml
  - path: ../../services/it-tools/docker-compose.yml
  - path: ../../services/excalidraw/docker-compose.yml

  # Monitoring
  - path: ../../services/monitoring/docker-compose.yml
  - path: ../../services/exporters/docker-compose.yml

services:
  # Main entry point for this node (Raspberry Pi 5)
  # High performance node for heavy workloads
  node-agent:
    <<: *defaults
    image: alpine:latest
    command: [ "/bin/sh", "-c", "echo 'Node active' && sleep infinity" ]

networks:
  monitoring:
    name: monitoring
    driver: bridge
