name: homelab-raspi5

x-defaults: &defaults
  restart: unless-stopped

services:
  placeholder:
    <<: *defaults
    env_file:
      - .env
    image: alpine:latest
    platform: linux/arm64
    command: ["/bin/sh", "-c", "sleep infinity"]
    # TODO: replace with services pinned for Raspberry Pi 5 (arm64, higher headroom)

